
spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: betgeris-dev
            name: client-service
      reload:
        enabled: true
        mode: event
        strategy: polling
