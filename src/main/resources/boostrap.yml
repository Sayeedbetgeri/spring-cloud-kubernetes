
spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: sayeed-test
            name: client-service
      reload:
        enabled: true
        mode: event
        strategy: polling
